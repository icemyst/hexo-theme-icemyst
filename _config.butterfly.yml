# --------------------------------------
# Hexo Butterfly Theme Configuration
# If you have any questions, please refer to the documentation
# Chinese: https://butterfly.js.org/
# English: https://butterfly.js.org/en/
# --------------------------------------

# --------------------------------------
# 导航设置
# --------------------------------------

nav:
  # 导航栏标志图片
  logo:
  display_title: true
  # 是否固定导航栏
  fixed: true

menu:
  文 章:
    隧道: /archives/ || fa-fw fas fa-archive
    分类: /categories/ || fa-fw fas fa-cubes
    标签: /tags/ || fa-fw fas fa-tags
  统 计:
    友人帐: /link/ || fas fa-link
    朋友圈: /fcircles/ || fa-brands fa-artstation
    统计表: /charts/ || fas fa-chart-line
    留言板: /comments/ || fa-fw fas fa-envelope
    清单: /todolist/ || fa-fw fas fa-table-list
  我 的:
    音乐馆: /music/?id=7657374410&server=tencent || fa-fw fas fa-music
    追番页: /bangumis/ || fa-fw fa-brands fa-bilibili
    相册集: /album/ || fa-fw fas fa-images
    藏宝阁: /collect/ || fa-fw fas fa-box
  关 于:
    关于本人: /about/ || fa-fw fas fa-dragon
    闲言碎语: /essay/ || fas fa-lightbulb
    随便逛逛: javascript:toRandomPost() || fas fa-shoe-prints

# --------------------------------------
# 代码块设置
# --------------------------------------

code_blocks:
  # 代码块主题: darker / pale night / light / ocean / false
  theme: light
  macStyle: true
  # 代码块高度限制 (unit: px)
  height_limit: 300
  word_wrap: false

  # 工具栏
  copy: true
  language: true
  # true：收缩代码块 | false：展开代码块 | none：展开代码块并隐藏按钮
  shrink: false
  fullpage: false

# 社交媒体链接
# Formal:
#   icon: link || the description || color
social:
  # fab fa-github: https://github.com/xxxxx || Github || '#24292e'
  # fas fa-envelope: mailto:xxxxxx@gmail.com || Email || '#4a7dbe'

# --------------------------------------
# 图像设置
# --------------------------------------

favicon: /img/favicon.ico

avatar:
  img: https://q1.qlogo.cn/g?b=qq&nk=1580777927&s=5
  effect: false

# 禁用所有横幅图像
disable_top_img: false

# 如果页面没有设置横幅，则显示default_top_img
default_top_img:

# 首页横幅图片
index_img: https://pic3.zhimg.com/80/v2-2d27325d3ef53714bd457bc2c40e8852_1440w.webp

# 档案页面的横幅图像
archive_img:

# 注意：标签页面，不是标签页
tag_img:

# 标签页的横幅图片，可以为每个标签设置横幅图片
# 格式:
#  - tag name: xxxxx
tag_per_img:

# 注意：分类页面，不是分类页面
category_img:

# 分类页的横幅图片，可以为每个分类设置横幅图片
# 格式:
#  - category name: xxxxx
category_per_img:

# 页脚背景图片
footer_img: transparent

# 网站背景
# 可以设置为颜色或图片网址
background: # https://api.boxmoe.com/random.php?size=large

cover:
  # 是否禁用封面
  index_enable: true
  aside_enable: true
  archives_enable: true
  # 未设置封面时，显示默认封面
  default_cover:
    - https://pic3.zhimg.com/80/v2-2d27325d3ef53714bd457bc2c40e8852_1440w.webp
    - https://pic4.zhimg.com/80/v2-5db05ca43bdb82d42f357cb581d384df_1440w.webp
    - https://pic2.zhimg.com/v2-ef87585088748f4e087489aaa8dcecbd_r.jpg


# 替换损坏的图像
error_img:
  flink: /img/friend_404.gif
  post_page: /img/404.png

# 一个简单的 404 页面
error_404:
  enable: true
  subtitle: '请尝试站内搜索寻找文章'
  background: https://pic.imgdb.cn/item/64c7bada1ddac507cc135dd8.jpg

post_meta:
  # 主页
  page:
    # 选择: created / updated / both
    date_type: both
    # 选择: date / relative
    date_format: relative
    categories: true
    tags: true
    label: true
  post:
    # 选择: left / center
    position: center
    # 选择: created / updated / both
    date_type: both
    # 选择: date / relative
    date_format: date
    categories: true
    tags: true
    label: true

# --------------------------------------
# 索引页设置
# --------------------------------------

# 首页 top_img 设置
# 默认：top img - 全屏，site info - 中间
# site info 位置，例如：300px/300em/300rem/10%
index_site_info_top:
# top_img 的高度，例如：300px/300em/300rem
index_top_img_height:

# 首页的副标题
subtitle:
  enable: true
  # 打字机效应
  effect: true
  # 自定义 typed.js
  # https://github.com/mattboldt/typed.js/#customization
  typed_option:
  # Source - 调用第三方服务 API（仅限中文）
  # 首先显示source，然后显示子内容的内容
  # 选择: false/1/2/3
  # false - 禁用该功能
  # 1 - hitokoto.cn
  # 2 - yijuzhan.com
  # 3 - jinrishici.com
  source: 1
  # 如果关闭打字机效果，字幕将只显示第一行字幕
  sub:

# 首页文章布局
# 1: 左边是封面，右边是信息
# 2: 右侧为封面，左侧为信息
# 3: 封面和信息在左右两侧交替
# 4: 封面在顶部，信息在底部
# 5: 封面上显示的信息
# 6：砌体布局 - 封面在顶部，信息在底部
# 7：砌体布局 - 信息显示在封面上
index_layout: 3

# 首页显示文章简介
# 1: 描述
# 2: 两者（如果描述存在，则显示描述，否则显示自动摘录）
# 3: auto_excerpt （默认）
# false: 不显示文章简介
index_post_content:
  method: 3
  # 如果将方法设置为 2 或 3，则长度需要配置
  length: 500

# --------------------------------------
# 文章设置
# --------------------------------------

toc:
  post: true
  page: false
  number: true
  expand: false
  # 仅适用于文章
  style_simple: false
  scroll_percent: true

post_copyright:
  enable: true
  decode: false
  author_href:
  license: CC BY-NC-SA 4.0
  license_url: https://creativecommons.org/licenses/by-nc-sa/4.0/

# 贊助/獎勵
reward:
  enable: true
  text: 打赏
  QR_code:
    - img: /img/wechat.webp
      link:
      text: 微信
    - img: /img/alipay.webp
      link:
      text: 支付宝

# 文章编辑
# 轻松在线浏览和编辑博客源代码。
post_edit:
  enable: false
  # url: https://github.com/user-name/repo-name/edit/branch-name/subdirectory-name/
  # For example: https://github.com/jerryc127/butterfly.js.org/edit/main/source/
  url:

# 相关文章
related_post:
  enable: true
  # 显示的文章数量
  limit: 6
  # 选择: created / updated
  date_type: created

# 选择：1 / 2 / false
# 1：‘下一篇文章’将链接到旧文章
# 2：‘下一篇文章’将链接到新文章
# false：禁用分页
post_pagination: 1

# 显示文章的过期通知
noticeOutdate:
  enable: true
  # 风格: simple / flat（简约/扁平）
  style: flat
  # 什么时候显示
  limit_day: 30
  # 位置: top / bottom（顶部/底部）
  position: top
  message_prev: 距离上次更新已经过去
  message_next: 天了，文章内容可能已经过时。若内容或图片失效，请留言反馈（部分素材来自网络）。

# --------------------------------------
# Footer Settings
# --------------------------------------
footer:
  owner:
    enable: true
    # 显示： 'By' / '🦋'（1 / 2），默认为 1
    source: 2
    since: 2019
  custom_text:
  # 主题和框架的版权
  copyright: true
  # 网站运行时间
  runtime:
    enable: true
    text: 本站已苟活 # 自定义文字内容
    launch_time: 04/01/2022 00:00:00 # 网站上线时间
    work_img: https://jsd.onmicrosoft.cn/gh/eudav/bingm-static/img/icon/冰梦-上班摸鱼中-6adea8.svg # https://img.shields.io/badge/冰梦-上班摸鱼中-6adea8?style=social&logo=snowflake
    work_description: 一个还在学习的萌新
    offduty_img: https://jsd.onmicrosoft.cn/gh/eudav/bingm-static/img/icon/冰梦-下班休息中-6adea8.svg # https://img.shields.io/badge/冰梦-下班休息中-6adea8?style=social&logo=neutralinojs
    offduty_description: 下班了就该开开心心的玩耍，嘿嘿~
    working_start: 9 # 上班时间
    working_end: 18 # 下班时间


  # 文章总字数统计
  wordcount: true

# --------------------------------------
# Aside Settings
# --------------------------------------

aside:
  enable: true
  hide: false
  # 在右下角按钮中显示隐藏按钮
  button: true
  mobile: true
  # 位置: left / right（左 / 右）
  position: right
  display:
    archive: true
    tag: true
    category: true
  card_author:
    enable: true
    description: 到不了的都叫远方，回不去的都叫故乡。
    button:
      enable: true
      icon: fas fa-arrow-circle-right
      text: 了解更多
      link: /about/
  card_announcement:
    enable: true
    content:
      - <div><a style='display:flex;height:100%;width:100%;' href='https://hcer.top'><img style="height:100%;width:100%;" src="https://jsd.onmicrosoft.cn/gh/eudav/bingm-static/img/hcer.webp" alt="https://hcer.top" style="border-radius:0;"></a></div>
        <b>GitHub站点：</b><a href='https://icemyst.github.io'>https://icemyst.github.io</a>
        <div>CDN赞助者：@xinpro(鸣谢)</div><div>赞助者博客：<a href="https://xinpro.cn">https://xinpro.cn</a></div>
  card_recent_post:
    enable: true
    # 如果设置为 0 将显示全部
    limit: 5
    # 排序: date / updated（日期 / 更新）
    sort: date
    sort_order:
  card_newest_comments:
    enable: false
    sort_order:
    limit: 6
    # 单位: mins（分钟）, 保存数据到本地存储
    storage: 10
    avatar: true
  card_categories:
    enable: true
    # 如果设置为 0 将显示全部
    limit: 8
    # 选择: none / true / false
    expand: none
    sort_order:
  card_tags:
    enable: true
    # 如果设置为 0 将显示全部
    limit: 40
    color: true
    # 标签顺序, random/name/length（随机/名称/长度）
    orderby: random
    # 排序顺序：1，表示升序；-1，表示降序
    order: 1
    sort_order:
  card_archives:
    enable: true
    # 类型: monthly / yearly（月度 / 年度）
    type: monthly
    # Eg: YYYY年MM月
    format: MMMM YYYY
    # 排序顺序：1，表示升序；-1，表示降序
    order: -1
    # 如果设置为 0 将显示全部
    limit: 8
    sort_order:
  card_post_series:
    enable: true
    # 标题显示系列名称
    series_title: false
    # 按标题或日期排序
    orderBy: 'date'
    # 排序顺序：1，表示升序；-1，表示降序
    order: -1
  card_webinfo:
    enable: true
    post_count: true
    last_push_date: true
    sort_order:
    # 发布日期与现在之间的时间差
    # 正式: Month/Day/Year Time or Year/Month/Day Time（月/日/年 时间或年/月/日 时间）
    # 如果不启用此功能，请留空
    runtime_date:

# --------------------------------------
# 右下角按钮
# --------------------------------------

# 右下角按钮与底部的距离（默认单位：px）
rightside_bottom:

# 繁体中文与简体中文转换
translate:
  enable: true
  # 按钮的文本
  default: 繁
  # 网站语言（1-繁体中文/ 2-简体中文）
  defaultEncoding: 2
  # 时间延迟
  translateDelay: 0
  # 语言为简体中文时按钮的文字
  msgToTraditionalChinese: '繁'
  # 语言为繁体中文时按钮的文字
  msgToSimplifiedChinese: '簡'

# 阅读模式
readmode: true

# 夜间模式
darkmode:
  enable: true
  # 切换按钮可切换深色/浅色模式
  button: true
  # 自动切换深色/浅色模式
  # autoChangeMode: 1 按照系统设置，若系统不支持暗黑模式，则会在晚上 6 点到早上 6 点之间切换为深色模式
  # autoChangeMode: 2 下午 6 点至早上 6 点之间切换深色模式
  # autoChangeMode: false
  autoChangeMode: false
  # 设置灯光模式时间，值在0~24之间，若不设置则默认为6~18
  start:
  end:

# 在滚动至顶部按钮中显示滚动百分比
rightside_scroll_percent: true

# 除非您知道以下设置如何工作，否则请不要修改它们
# 选择: readmode,translate,darkmode,hideAside,toc,chat,comment（阅读模式，翻译，深色模式，隐藏，目录.聊天，评论）
# 不要重复相同的值
rightside_item_order:
  enable: false
  # 默认: readmode,translate,darkmode,hideAside（阅读模式，翻译，深色模式，隐藏）
  hide:
  # 默认: toc,chat,comment（目录，聊天，评论）
  show:

# --------------------------------------
# 全局设置
# --------------------------------------

anchor:
  # 当你滚动时，URL 将根据标题 ID 进行更新。
  auto_update: false
  # 点击标题滚动并更新锚点
  click_to_scroll: false

# 图片描述文字
photofigcaption: false

copy:
  enable: true
  # 在复制内容后添加版权信息
  copyright:
    enable: true
    limit_count: 50

# 需要安装hexo-wordcount插件
wordcount:
  enable: true
  # 在文章元信息中显示字数
  post_wordcount: true
  # 在文章元信息中显示阅读时间
  min2read: true
  # 在侧边栏的站点信息中显示网站的总字数
  total_wordcount: true

# Busuanzi count for PV / UV in site
busuanzi:
  site_uv: true
  site_pv: true
  page_pv: true

# --------------------------------------
# Math
# --------------------------------------

# About the per_page
# 如果将其设置为 true，它将在每个页面中加载 mathjax/katex 脚本
# 如果将其设置为 false，它将根据您的设置加载 mathjax/katex 脚本（在页面的正面添加“mathjax：true”或“katex：true”）
math:
  # 选择：mathjax、katex
  # 如果不需要数学运算，请将其留空
  use:
  per_page: true
  hide_scrollbar: false

  mathjax:
    # 启用上下文菜单
    enableMenu: true
    # 选择: all / ams / none（全部 / 美式数学 / 无）, 这决定了方程是否编号以及如何编号
    tags: none

  katex:
    # 启用复制 KaTeX 公式
    copy_tex: false

# --------------------------------------
# 搜索
# --------------------------------------

search:
  # 选择: algolia_search / local_search / docsearch
  # 如果不需要搜索，请留空
  use: algolia_search
  placeholder:

  # Algolia Search
  algolia_search:
    # 每页搜索结果数
    hitsPerPage: 6

  # Local Search
  local_search:
    # 在页面加载时预加载搜索数据
    preload: false
    # 显示每篇文章的前 n 个结果，设置为 -1 则显示所有结果
    top_n_per_article: 1
    # 将 HTML 字符串转换为可读字符串。
    unescape: false
    CDN:

  # Docsearch
  # https://docsearch.algolia.com/
  docsearch:
    appId:
    apiKey:
    indexName:
    option:

# --------------------------------------
# 共享设置
# --------------------------------------

share:
  # 选择: sharejs / addtoany
  # 如果不需要共享，可留空
  use: sharejs

  # Share.js
  # https://github.com/overtrue/share.js
  sharejs:
    sites: facebook,twitter,wechat,weibo,qq

  # AddToAny
  # https://www.addtoany.com/
  addtoany:
    item: facebook,twitter,wechat,sina_weibo,facebook_messenger,email,copy_link

# --------------------------------------
# 评论设置
# --------------------------------------

comments:
  # 最多两条评论系统，第一条将默认显示
  # 如果不需要评论，可留空
  # 选择: Disqus/Disqusjs/Livere/Gitalk/Valine/Waline/Utterances/Facebook Comments/Twikoo/Giscus/Remark42/Artalk
  # 显示两个评论设置: Disqus,Waline
  use: Twikoo
  # 在按钮旁边显示评论名称
  text: true
  # Lazyload：当评论元素进入浏览器视口时，评论系统才会加载。
  # 如果设置为 true，评论数将无效
  lazyload: false
  # 在文章的 top_img 中显示评论数
  count: false
  # 在主页显示评论数
  card_post_count: false

# Disqus
# https://disqus.com/
disqus:
  shortname:
  # 用于最新评论小部件
  apikey:

# 替代 Disqus - 使用 Disqus API 呈现评论
# https://github.com/SukkaW/DisqusJS
disqusjs:
  shortname:
  apikey:
  option:

# Livere
# https://www.livere.com/
livere:
  uid:

# Gitalk
# https://github.com/gitalk/gitalk
gitalk:
  client_id:
  client_secret:
  repo:
  owner:
  admin:
  option:

# Valine
# https://valine.js.org
valine:
  appId:
  appKey:
  avatar: monsterid
  # 此配置适用于国内自定义域名用户，海外版本会自动检测（无需手动填写）
  serverURLs:
  bg:
  # 使用 Valine 的访客计数作为页面浏览量统计
  visitor: false
  option:

# Waline - 一个简单的评论系统，具有后端支持，基于 Valine 的分支开发
# https://waline.js.org/
waline:
  serverURL:
  bg:
  # 使用 Waline 的页面浏览计数作为页面浏览量统计
  pageview: false
  option:

# Utterances
# https://utteranc.es/
utterances:
  repo:
  # 问题映射：pathname（路径名）/url（链接地址）/title（标题）/og:title（开放图谱标题）
  issue_term: pathname
  # 主题: github-light/github-dark/github-dark-orange/icy-dark/dark-blue/photon-dark
  light_theme: github-light
  dark_theme: photon-dark
  js:
  option:

# Facebook 评论插件
# https://developers.facebook.com/docs/plugins/comments/
facebook_comments:
  app_id:
  # optional
  user_id:
  pageSize: 10
  # 选择: social / time / reverse_time
  order_by: social
  lang: en_US

# Twikoo
# https://github.com/imaegoo/twikoo
twikoo:
  envId: https://twikoo.abeginner.top/.netlify/functions/twikoo
  region:
  # 使用 Twikoo 的访客计数作为页面浏览量统计
  visitor: false
  option:

# Giscus
# https://giscus.app/
giscus:
  repo:
  repo_id:
  category_id:
  light_theme: light
  dark_theme: dark
  js:
  option:

# Remark42
# https://remark42.com/docs/configuration/frontend/
remark42:
  host:
  siteId:
  option:

# Artalk
# https://artalk.js.org/guide/frontend/config.html
artalk:
  server:
  site:
  # 使用 Artalk 的访客计数作为页面浏览量统计
  visitor: false
  option:

# --------------------------------------
# 聊天服务
# --------------------------------------

chat:
  # 选择: chatra/tidio/crisp
  # 如果不需要聊天，可留空
  use:
  # 聊天按钮 [推荐]
  # 它将在网站右下角创建一个按钮，并隐藏来源按钮
  rightside_button: false
  # 向上滚动时显示原始聊天按钮，向下滚动时隐藏该按钮
  button_hide_show: false

# https://chatra.io/
chatra:
  id:

# https://www.tidio.com/
tidio:
  public_key:

# https://crisp.chat/en/
crisp:
  website_id:

# --------------------------------------
# 分析
# --------------------------------------

# https://tongji.baidu.com/web/welcome/login
baidu_analytics: ef832811ada62c5e8a9972ba80c825c0

# https://analytics.google.com/analytics/web/
google_analytics:

# https://www.cloudflare.com/zh-tw/web-analytics/
cloudflare_analytics:

# https://clarity.microsoft.com/
microsoft_clarity:

# https://umami.is/
umami_analytics:
  enable: false
  # 对于自托管设置，配置 Umami 实例的主机名
  serverURL:
  website_id:
  option:
  UV_PV:
    site_uv: false
    site_pv: false
    page_pv: false
    # Umami Cloud (API key) / self-hosted Umami (token)
    token:

# --------------------------------------
# 广告
# --------------------------------------

# Google 广告
google_adsense:
  enable: false
  auto_ads: true
  js: https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js
  client:
  enable_page_level_ads: true

# 手动插入广告
# 如果不需要广告则留空
ad:
  # 在首页插入广告（每三篇文章插入一次）
  index:
  # 在侧栏插入广告
  aside:
  # 在文章中插入广告（分页之前）
  post:

# --------------------------------------
# Verification
# --------------------------------------

site_verification:
  # - name: google-site-verification
  #   content: xxxxxx
  # - name: baidu-site-verification
  #   content: xxxxxxx

# --------------------------------------
# Beautify / Effect
# --------------------------------------

# 自定义主题颜色
# 注意：颜色值必须用双引号括起来，如“#000”，否则可能会出错！

# theme_color:
#   enable: true
#   main: "#49B1F5"
#   paginator: "#00c4b6"
#   button_hover: "#FF7242"
#   text_selection: "#00c4b6"
#   link_color: "#99a9bf"
#   meta_color: "#858585"
#   hr_color: "#A4D8FA"
#   code_foreground: "#F47466"
#   code_background: "rgba(27, 31, 35, .05)"
#   toc_color: "#00c4b6"
#   blockquote_padding_color: "#49b1f5"
#   blockquote_background_color: "#49b1f5"
#   scrollbar_color: "#49b1f5"
#   meta_theme_color_light: "ffffff"
#   meta_theme_color_dark: "#0d0d0d"

# 分类和标签页的用户界面设置
# 选择：index - 与首页 UI 相同 / default - 与档案 UI 相同
# 留空或index
category_ui:
tag_ui:

# UI 元素的圆角
rounded_corners_ui: true

# 拉伸线条，使每条线条宽度相等
text_align_justify: false

# 为页眉和页脚添加蒙版
mask:
  header: true
  footer: true

# 加载动画
preloader:
  enable: true
  # source
  # 1. fullpage-loading
  # 2. pace (progress bar)
  source: 2
  # pace theme (see https://codebyzach.github.io/pace/)
  pace_css_url:

# 页面过渡
enter_transitions: true

# 默认显示模式 - light (default) / dark
display_mode: light

# 文章内容美化配置
beautify:
  enable: false
  # 指定要美化的字段 (site（站点） or post（文章）)
  field: post
  # 指定用作标题前缀的图标，例如 '\f0c1'
  title_prefix_icon:
  # 指定标题前缀图标的颜色，例如 '#F47466'
  title_prefix_icon_color:

# 全局字体设置
# 除非您知道以下设置如何工作，否则请不要修改它们
font:
  global_font_size:
  code_font_size: 16px
  font_family:
  code_font_family: Arial,-apple-system,BlinkMacSystemFont,'Microsoft YaHei','Segoe UI','Helvetica Neue',Lato,Roboto,'PingFang SC',sans-serif

# 站点标题和副标题的字体设置
blog_title_font:
  font_link:
  font_family: Arial,PingFang SC, 'Hiragino Sans GB', 'Microsoft JhengHei', 'Microsoft YaHei', sans-serif

# 分隔线图标的设置
hr_icon:
  enable: true
  # Font Awesome 图标的 unicode 值，例如 '\3423'
  icon:
  icon_top:

# 打字机效果
# https://github.com/disjukr/activate-power-mode
activate_power_mode:
  enable: false
  colorful: true
  shake: true
  mobile: false

# 背景效果
# --------------------------------------

# canvas_ribbon
# See: https://github.com/hustcc/ribbon.js
canvas_ribbon:
  enable: false
  # 彩带的大小
  size: 150
  # 彩带的不透明度（0~1）
  alpha: 0.6
  zIndex: -1
  click_to_change: false
  mobile: false

# 飘动的彩带
canvas_fluttering_ribbon:
  enable: false
  mobile: false

# canvas_nest
# https://github.com/hustcc/canvas-nest.js
canvas_nest:
  enable: false
  # 线条颜色，默认：'0,0,0'；RGB 值：(R,G,B)。（注意：使用','分隔。）
  color: '0,0,255'
  # 线条的不透明度（0~1）
  opacity: 0.7
  # 背景的 z-index 属性
  zIndex: -1
  # 行数
  count: 99
  mobile: false

# 鼠标点击效果：烟花
fireworks:
  enable: false
  zIndex: 9999
  mobile: false

# 鼠标点击效果：心形符号
click_heart:
  enable: false
  mobile: false

# 鼠标点击效果：文字
clickShowText:
  enable: false
  text:
    # - I
    # - LOVE
    # - YOU
  fontSize: 15px
  random: false
  mobile: false

# --------------------------------------
# 灯箱设置
# --------------------------------------

# 选择: fancybox / medium_zoom
# https://github.com/francoischalifour/medium-zoom
# https://fancyapps.com/fancybox/
# 如果不需要灯箱，请将其留空
lightbox: fancybox

# --------------------------------------
# 标签插件设置
# --------------------------------------

# Series
series:
  enable: false
  # 按标题或日期排序（title / date）
  orderBy: 'title'
  # 排序顺序：1，表示升序；-1，表示降序
  order: 1
  number: true

# ABCJS - ABC 音乐符号插件
# https://github.com/paulrosen/abcjs
abcjs:
  enable: false
  per_page: true

# Mermaid
# https://github.com/mermaid-js/mermaid
mermaid:
  enable: false
  # Write Mermaid diagrams using code blocks
  code_write: false
  # 内置主题：default / forest / dark / neutral
  theme:
    light: default
    dark: dark

# chartjs
# see https://www.chartjs.org/docs/latest/
chartjs:
  enable: false
  # 除非您了解它们的工作原理，否则请不要进行修改。
  # 仅当未指定 MD 语法时才使用默认设置。
  # 图表的常规字体颜色
  fontColor:
    light: 'rgba(0, 0, 0, 0.8)'
    dark: 'rgba(255, 255, 255, 0.8)'
  # 图表的常规边框颜色
  borderColor:
    light: 'rgba(0, 0, 0, 0.1)'
    dark: 'rgba(255, 255, 255, 0.2)'
  # 雷达和极地区域图上刻度标签的背景颜色
  scale_ticks_backdropColor:
    light: 'transparent'
    dark: 'transparent'

# 注意 - Bootstrap 标注
note:
  # 注释标签样式值:
  #  - simple    bs-callout 旧警告样式。默认。
  #  - modern    s-callout 新的 (v2-v3) 警告样式
  #  - flat      带背景的扁平化提示样式，类似于Mozilla或StackOverflow上的样式
  #  - disabled  禁用注释标签的所有 CSS 样式导入。
  style: flat
  icons: true
  border_radius: 3
  # 现代风格和扁平风格背景颜色的偏移亮度百分比（现代风格：-12 | 12；扁平风格：-18 | 6）。
  # 偏移也应用于标签（label）标签变量。此选项可以与禁用的注释标签（note tag）一起使用。
  light_bg_offset: 0

# --------------------------------------
# 其他设置
# --------------------------------------

# https://github.com/MoOx/pjax
pjax:
  enable: true
  # 将指定页面排除在 pjax 之外，例如 '/music/
  exclude:
    # - /xxxxxx/

# 注入 CSS 和脚本（aplayer/meting）
aplayerInject:
  enable: false
  per_page: true

# Snackbar - Toast 通知
# https://github.com/polonel/SnackBar
# 位置: top-left / top-center / top-right / bottom-left / bottom-center / bottom-right
snackbar:
  enable: true
  position: top-center
  # Toast 通知在浅色模式和深色模式下的背景颜色
  bg_light: '#49b1f5'
  bg_dark: '#1f1f1f'

# Instant.page
# https://instant.page/
instantpage: true

# Pangu - 在汉字和英文字母之间插入空格
# https://github.com/vinta/pangu.js
pangu:
  enable: true
  # 指定使用 Pangu 的字段（站点（site）或文章（post））
  field: site

# Lazyload
# https://github.com/verlok/vanilla-lazyload
lazyload:
  enable: true
  # 指定使用懒加载的字段（站点（site）或文章（post））
  field: site
  placeholder: /img/loading.webp
  blur: false

# PWA
# See https://github.com/JLHwung/hexo-offline
# ---------------
pwa:
  enable: true
  manifest: /manifest.json
  apple_touch_icon: /img/siteicon/apple-touch-icon.png
  favicon_32_32: /img/siteicon/favicon-32x32.png
  favicon_16_16: /img/siteicon/favicon-16x16.png
  mask_icon: /img/siteicon/apple-touch-icon.png

# Open Graph 元标签
# https://hexo.io/docs/helpers#open-graph
Open_Graph_meta:
  enable: true
  option:
    # twitter_card:
    # twitter_image:
    # twitter_id:
    # twitter_site:
    # google_plus:
    # fb_admins:
    # fb_app_id:

# 添加厂商前缀以确保兼容性
css_prefix: true

# Inject
# 将代码插入到头部（</head> 标签前）和底部（</body> 标签前）
inject:
  head:
    # - <link rel="stylesheet" href="/xxx.css">
  bottom:
    - <script async src="//at.alicdn.com/t/font_2032782_8ns648avijk.js"></script> # 阿里云图标库

# CDN 设置
# 除非你知道它们的作用，否则不要修改以下设置
CDN:
  # 内部和第三方脚本的 CDN 提供商
  # 可选项：local/jsdelivr/unpkg/cdnjs/custom
  # 注意：开发版本只能使用‘local’来加载内部脚本
  # 注意：将第三方脚本设置为‘local’时，需要安装 hexo-butterfly-extjs
  internal_provider: onmicrosoft
  third_party_provider: onmicrosoft

  # 是否在 URL 中添加版本号, true or false

  # 自定义格式
  # For example: https://cdn.staticfile.org/${cdnjs_name}/${version}/${min_cdnjs_file}
  custom_format: https://npm.elemecdn.com/${name}@latest/${file}

  option:
    # abcjs_basic_js:
    # activate_power_mode:
    # algolia_js:
    # algolia_search:
    # aplayer_css:
    # aplayer_js:
    # artalk_css:
    # artalk_js:
    # blueimp_md5:
    # busuanzi:
    # canvas_fluttering_ribbon:
    # canvas_nest:
    # canvas_ribbon:
    # chartjs:
    # click_heart:
    # clickShowText:
    # disqusjs:
    # disqusjs_css:
    # docsearch_css:
    # docsearch_js:
    # egjs_infinitegrid:
    # fancybox:
    # fancybox_css:
    # fireworks:
    # fontawesome:
    # gitalk:
    # gitalk_css:
    # giscus:
    # instantpage:
    # instantsearch:
    # katex:
    # katex_copytex:
    # lazyload:
    # local_search:
    # main:
    # main_css:
    # mathjax:
    # medium_zoom:
    # mermaid:
    # meting_js:
    # pangu:
    # prismjs_autoloader:
    # prismjs_js:
    # prismjs_lineNumber_js:
    # pjax:
    # sharejs:
    # sharejs_css:
    # snackbar:
    # snackbar_css:
    # translate:
    # twikoo:
    # typed:
    # utils:
    # valine:
    # waline_css:
    # waline_js:
